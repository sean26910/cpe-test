1. if (d > dist[cur]) continue;

作用：過期路徑丟棄（Lazy Deletion）

在 Dijkstra 演算法中，同一個節點可能會被多次推入 priority_queue。

想像一下這個場景：

一開始找到一條路徑到節點 $A$，距離是 10。此時 pq 裡有 {10, A}。

稍後，你又找到一條更短的路徑到節點 $A$，距離是 5。此時你更新了 dist[A] = 5，並把 {5, A} 也推入了 pq。

現在 pq 裡同時存在 {5, A} 和 {10, A}。

當 pq.top() 執行時，它會先彈出較小的 {5, A}。處理完後，dist[A] 已經是 5 了。

等到後來彈出 {10, A} 時，你會發現這個 $d=10$ 比目前的 dist[A]=5 還要大。

這代表：這個 {10, A} 是一條「過時」且「比較爛」的路徑。

如果不加這個 if 判斷，程式會拿這條比較爛的路徑去更新周圍的鄰居，導致大量的重複運算。加上 continue 就能直接跳過，大幅提升效率。

2. if (dist[next] > dist[cur] + w) 內部才執行 pq.push

作用：鬆弛操作（Relaxation）與減少無用任務

你問：「為什麼只在發現更短路徑時才 push？」

因為 Dijkstra 的目標是尋找最短路徑。

只有當 dist[cur] + w（經過當前節點 cur 到達 next 的距離）比 dist[next]（目前紀錄到 next 的最優距離）還要小的時候，才代表我們找到了一條「更有潛力」的新路徑。

如果不 push：如果新路徑比較長，它絕對不會成為最短路徑的一部分，推入 pq 只會浪費記憶體和處理時間。

只在更新時 push：這能保證 pq 裡的資料都是當下最有可能優化結果的「候選者」。
