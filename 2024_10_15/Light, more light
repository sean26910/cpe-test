#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <fstream>
#include <ostream>
#include <map>
#include <queue>
#include <climits>
#include <stack>
#include <iomanip>
#include <numeric>
#include <set>
#include <algorithm>
#include <string.h>
#include <sstream>
using namespace std;

struct cows
{
	int cycle;
	vector<int> mi;
	bool alive;
};

int main() {
    long long n;
	while(cin>>n)
	{
		if(n==0)
			break;

		long long x = sqrt(n);

		if(x*x==n)
			cout<<"yes\n";
		else
			cout<<"no\n";
	}
}
